# Project Log — HarborVale Workflow

Decisions, experiments, metrics, failures, lessons. Newest entries last.
Decision ids: `D-M<milestone>-<n>`. Design-level decisions D1–D12 live in `DESIGN_HE.html`.

## 2026-09-08 — Planning (design)
- Read the brief (`final_projec.html`): CrewAI Flow coordinating a Data Analyst crew (≥3 agents, 4 artifacts) and a Data Scientist crew (≥3 agents, 4 artifacts), with a machine-enforced `dataset_contract.json` at the seam; Flask/Streamlit app; public repo with PRs; deployment optional.
- Reviewed how Dor works from Northwind and FunnelIQ (CLAUDE.md, PLAN.md, PROJECT_LOG.md, reports) and wrote `CLAUDE.md` for this project.
- Environment facts verified: `crewai` 1.15.16 exists only in anaconda `base` (3.13); `AI_dev` (3.11) lacks it; no Kaggle CLI or credentials; `gh` logged in as DorKatzav; the git root of this folder is the whole course repo.
- Locked with Dor in conversation: solo for now (teammate possible later); dataset = E-commerce Customer Churn (Kaggle, ankitverma2010); new conda env `harborvale` (Python 3.11); Flask + HTML/CSS (full deployment wanted); Railway; app = public display + live validation + "break it" panel, with a password-protected live full run as a later milestone; model gpt-5-mini.
- Cost estimate given (published prices 2026-09-08): gpt-5-mini $0.25 / $2.00 per 1M tokens → ~$0.10–0.30 per full run with agents calling deterministic tools; Railway Hobby $5/month incl. $5 usage.
- Design doc written and verified RTL in the browser: `DESIGN_HE.html` (decisions D1–D12).

## 2026-09-08 — Planning (approved)
- Dor approved D1–D12 ("מאשר הכל"): own repo + public GitHub `harborvale-workflow`; conda `harborvale` 3.11 pinned; raw xlsx committed read-only; agents orchestrate + write, `hv/` tools compute; own JSON contract + Python validator, measured fields from the tool and human fields from the agent, all failures reported together; fail = stop with FAILED.md + exit 2, no auto-retry; nulls kept and declared, imputed in Crew 2's Pipeline; Gender/MaritalStatus not features + fairness in the model card; Crew 2 file access sandboxed to crew1 artifacts; gpt-5-mini, stubs in tests/CI; one Flask service on Railway, live run behind password in M7; M0–M8 + optional M9.
- `PLAN.md` (contracts §3, milestones §4 with gates, protocol §5) and `PLAN_HE.html` written. Obsidian project pages created.
- Next: M0 (skeleton deployed). Dor's manual step for M0: create the Railway service from the GitHub repo and paste the domain into `.env` as `LIVE_URL`.

## 2026-09-08 — M0: deployed skeleton — code complete, GATE 4/4 local (live check pending Railway)
- Own git repo initialised (course repo ignores the folder). GitHub: https://github.com/DorKatzav/harborvale-workflow (public). Branch `feat/m0-skeleton`, PR #1.
- conda env `harborvale` (Python 3.11.16): CrewAI **1.15.20** (pip resolved newer than base's 1.15.16), pandas 3.0.5, scikit-learn 1.9.0, Flask 3.1.3, pydantic 2.12.5, openai 2.54.0, gunicorn 26.2.0, pytest 9.1.1, ruff 0.16.6 — all pinned in `requirements.txt`.
- `hv/config.py` (paths, SEED, roles, ENTITY_MAP, UNITS, CSV_KW, get_llm), Flask `/health` (version, commit from `RAILWAY_GIT_COMMIT_SHA` or git, model_loaded, uptime), 8 tests, ruff, CI (OPENAI_API_KEY empty on purpose), Railway config with an explicit gunicorn start command (`--workers 1 --threads 4` so the M7 runner state is shared), `scripts/gate.py --m 0` with a key-material-only secret scan.
- Gate: PASS 4/4; live `/health` SKIPPED until Dor creates the Railway service and sets `LIVE_URL`. Local gunicorn smoke test returned `/health` with commit 2671cb5.
- Lesson: `ruff format --check` also formats code fences inside Markdown files; CI runs `ruff check` only.
- CI incident: first run failed with `ModuleNotFoundError: hv` — bare `pytest` on Actions does not put the repo root on `sys.path` (local `python -m pytest` does). Fixed with `pyproject.toml` `[tool.pytest.ini_options] pythonpath = ["."]`.
- PR #1 merged by Dor's approval (main 8951b3f). Railway service created by Dor; domain `web-production-82f247.up.railway.app`.
- Incident caught by the gate: the OpenAI key and LIVE_URL were pasted into `.env.example` (tracked) instead of `.env` (absent). Secret scan FAILED before any commit; content moved to `.env` (verified gitignored), `.env.example` restored from git. Never reached the repo. Rotation offered to Dor as optional.
- Second small fix: LIVE_URL pasted without a scheme (Railway shows the bare host) → gate prepends `https://` when missing.
- **GATE M0: PASS 5/5.** Live `/health` returns commit 8951b3f2b774 == origin/main, version 0.1.0, model_loaded false.
- Next: M1 — Crew-1 tools (ingest, cleaning, EDA). Dor: download the Kaggle file to `data/raw/ecommerce_churn.xlsx`.

## 2026-09-08 — M1 prerequisite: raw dataset committed
- Dor downloaded the Kaggle file; it landed as a single file named `data:raw:ecommerce_churn.xlsx` in the project root (macOS turns `/` into `:` in save dialogs). Moved to `data/raw/ecommerce_churn.xlsx` (555,610 bytes, sha256 db70f1e3…1bd44d). Never modified from here on.
- Sheets verified against `hv/config.py`: `E Comm` (5,630 data rows × 20 columns, header starts CustomerID, Churn, Tenure, PreferredLoginDevice, CityTier, WarehouseToHome) and `Data Dict` (22 × 4). `RAW_SHEET` / `DICT_SHEET` need no change.
- Onboarding page for a second contributor merged (PR #3): `docs/notes/ONBOARDING_HE.html`.

## 2026-09-08 — M1: Crew-1 tools (ingest, cleaning, EDA) — GATE PASSED (7/7), D-M1-1 pending approval
- Branch `feat/m1-analyst-tools`, PR #5. Tests first (synthetic 8-row frame with every kind of dirt), then `hv/ingest.py`, `hv/cleaning.py`, `hv/eda.py`; 21 new tests (29 total).
- Raw file facts (from `ingest.profile`): 5,630 × 20; 0 exact duplicate rows; 0 duplicate ids; **556 rows identical in every column except CustomerID**; nulls Tenure 264, WarehouseToHome 251, HourSpendOnApp 255, OrderAmountHikeFromlastYear 265, CouponUsed 256, OrderCount 258, DaySinceLastOrder 307; churn 16.84%.
- Entity map fixes: Phone→Mobile Phone 1,231; CC→Credit Card 273; COD→Cash on Delivery 365; Mobile→Mobile Phone 809. Entity map runs before duplicate detection: one extra pair became identical after the CC/Credit Card merge (556 raw → 557 dropped).
- Clean file: 5,073 rows, churn 16.58% (841), nulls kept (Tenure 231, WarehouseToHome 221, HourSpendOnApp 230, OrderAmountHike 252, CouponUsed 210, OrderCount 243, DaySinceLastOrder 288), sha256 ef1e1f758009fa57…; byte-identical across two runs (`stats.json` too).
- Findings (stats.json): tenure dominates — 0–1 months 51.3% churn (n 1,076) vs 7.4% / 5.7% / 5.3% for 2–6 / 7–12 / 13+; mean tenure 3.5 months (churned) vs 11.5 (stayed), r = −0.34. Complaint: 31.3% vs 10.8%, r = +0.25. Mobile Phone order category 26.3% vs Grocery 4.4%; Single 26.7% vs Married 11.3%; CityTier 3 21.9% vs tier 1 14.0%; Cash on Delivery 25.4%, E wallet 22.8%; churned customers ordered more recently (3.3 vs 4.8 days) and got less cashback (161.8 vs 180.8). Coupons: r ≈ 0.
- **D-M1-1 (proposed, pending Dor):** drop rows identical in all columns except CustomerID, keep the lowest id (default). Alternative B: keep them (`clean(drop_duplicate_records=False)`, `--keep-duplicate-records`). Rationale: identical records across CV folds inflate metrics by memorisation; cost: 557 rows (9.9%), churn rate 16.84% → 16.58%.
- Gate M1 7/7 (rows match report, no unmapped spellings, nulls exact, two runs identical, EDA self-contained). Report `docs/reports/M1_HE.html` with a screenshot of the generated EDA report.
- Lessons: pandas 3 string columns are dtype `str` (use `is_string_dtype`); skip constant columns before Pearson; entity map must run before duplicate detection.
- Housekeeping: the partner message (`docs/notes/PARTNER_SPLIT_MESSAGE.md`) was accidentally staged by `git add -A` and untracked again on the branch; it stays local.
- **D-M1-1 APPROVED by Dor (option A):** duplicate records dropped, lowest id kept. Clean file stays at 5,073 rows. PR #5 merged.

## 2026-09-08 - M2: the contract and the validator - GATE PASSED (5/5)
- Branch `feat/m2-contract`, PR #7. Second contributor (Yoran, track B) starts here; built on a synthetic 20-row frame (`tests/synthetic.py`) so M2 did not wait for M1, then re-run against the real artifacts once M1 merged.
- `hv/contract.py` (PLAN.md 3.5): models, `build_contract`, `apply_human_fields`, save/load, `validate`, `tamper`. 74 new tests (103 total): every one of the nine checks has a test that makes it fail, every preset has a test asserting it trips exactly its own check.
- Real contract: 5,073 rows, 20 columns, positive rate 0.1658, sha256 ef1e1f758009fa57...; roles 1 id / 1 target / 16 feature / 2 protected; dtypes 7 int / 8 float / 5 category; 7 nullable columns, 1,675 declared nulls; 20/20 descriptions from the raw file's data dictionary; 65 checks pass on the real file.
- Seam check that mattered: the contract built from the in-memory clean frame (what the M3 agent will do) is identical to the one built from the CSV read back off disk. If those had differed, M3 would have broken with no obvious cause.
- **D-M2-1: `.gitattributes` with `* text=auto eol=lf`.** A Windows checkout turned the committed `clean_data.csv` into CRLF, so its sha256 was 96f2d52f here vs ef1e1f75 on Dor's mac and in the M1 log. The contract stores that hash, so the integrity check would have failed on whichever machine did not build the contract, and on Linux CI. Alternatives: `*.csv -text` only (narrower, leaves the next text artifact unprotected); each Windows contributor setting `core.autocrlf=input` (not enforced by the repo). Cost: one new file; verified that renormalising changes no existing blob, so Dor sees no diff.
- **D-M2-2: `dtype_change` writes labels, not digits.** `CityTier.astype(str)` writes "1", which `read_csv` reads straight back as an int - the break was invisible on the only path that matters, and the gate failed 4/5 because of it. Now `Tier 1`/`Tier 2`/`Tier 3`. Alternatives: validate that preset in memory (inconsistent CLI, special case in the M6 panel); accept as a documented limit (gate green while one of six demos does not do what it advertises). Cost: one line of PLAN.md 3.5, updated in the same PR. Approved by Yoran.
- Incident, Windows-only, in M1 territory and left alone: `gate.py --m 1` fails here with "stats.json differs between runs". Content identical, line endings not - `hv/eda.py::write_stats` calls `Path.write_text` without `newline=""`, so Python rewrites 
 as 
 (200 of them). One-word fix for Dor: `write_text(..., encoding="utf-8", newline="")`. `save_contract` passes `newline="
"` explicitly and is not affected.
- Lesson: this Windows console is cp1255; printing the em-dash and the ~= character raised `UnicodeEncodeError`, including in `gate.py`'s existing SKIP/FAIL lines, which M2's gate hits four times. Everything printed is ASCII now; docstrings and reports untouched.
- Gate M2 5/5: tests green, the real clean file passes its real contract, all six presets caught through `scripts/break_it.py` with the x100 hint, a description cannot change the verdict, the contract round-trips unchanged. Report `docs/reports/M2_HE.html`.
- Next: M3 (Dor, Crew 1 in CrewAI) and M4 (Yoran, Crew-2 tools) run in parallel; they meet at M5.
## 2026-09-08 — M3: Crew 1 in CrewAI (two agents, three tasks) — real run done, GATE pending
- Scope note (deviation from PLAN §4 M3, logged): the Data Steward's `author_contract` task needs `hv/contract.py` from M2, which is not built yet. M3 ships the Data Quality Engineer + Business Analyst with `profile_and_clean → explore → write_insights`; the steward agent is declared in `agents.yaml` and its task + tools land as **M3b** right after M2 merges. The brief's "≥3 agents" is met at M3b.
- Built: `crews/analyst/tools.py` (4 CrewAI tools wrapping hv; errors returned as strings so the agent can retry), `config/agents.yaml` + `config/tasks.yaml`, `crews/analyst/crew.py` (`@CrewBase AnalystCrew`, sequential, `max_iter=8`, no delegation, no temperature), `hv/insights.py` (key-numbers table rendered from stats.json + cleaning_report.json; agent supplies five sections; `key_numbers_match` for the gate), `crews/stubs.py` (same tools, canned prose, no LLM; CLI used by the gate), `hv.config.estimate_cost_usd`. Temporary `scripts/run_crew1_tools.py` removed. 14 new tests (43 total), all green with `OPENAI_API_KEY` unset.
- **Real run (gpt-5-mini, `runs/20260908-191703/crew1`):** 7 LLM calls, 12,385 prompt tokens (3,456 cached), 7,139 completion tokens, **cost $0.0166**, **duration 96.4 s**. All five files produced on the first pass; `write_insights` accepted the sections on the first call (no ERROR round-trip).
- Determinism: `clean_data.csv`, `cleaning_report.json` and `stats.json` from the agent run are byte-identical to the M1 commit (tools, not agents, produce them). `insights.md` prose is the agent's; every figure spot-checked against stats.json (51.3% first-month churn n=1,076; complaint 31.3% vs 10.8%; Grocery 4.4% n=366; COD 25.4% n=457; E wallet 22.8% n=562; Credit Card 14.2% n=1,596; Single 26.7% / Married 11.3%; correlations −0.34 / +0.25 / −0.15 / −0.14 / +0.12).
- Observation: the agent wrote "5630 rows" without a thousands separator once, and copied the tool's JSON verbatim as instructed. Prompt cost is dominated by the tool JSON echoed into context (stats is ~4 KB); fine at this size.

## 2026-09-08 — M3b: the Data Steward's contract task — GATE M3 PASSED (8/8), M3 complete
- Branch `feat/m3b-steward-contract`. Adds to Crew 1: `build_contract_measured` + `write_contract_human_fields` tools (over Yoran's `hv/contract.py`), the `data_steward` agent and the `author_contract` task, the stub's contract step, gate M3 checks 3–4 (contract validates the clean file; measured fields equal a fresh build and the steward wrote rationale + assumptions), `measured_fields()` helper, and the Windows newline fix (`newline="\n"` on every `write_text` in hv/ and crews/). PLAN.md §4 gets an M3b note. 7 new tests (121 total).
- **Real run (gpt-5-mini, 3 agents / 4 tasks, `runs/20260908-205214/crew1`):** 10 LLM calls, 27,683 prompt tokens (11,776 cached), 16,389 completion tokens, **cost $0.037**, **duration 176.8 s**. Six files produced. One tool ERROR round-trip: the steward's first `write_contract_human_fields` call carried malformed JSON ("Extra data" after the descriptions object); the tool answered `ERROR: invalid JSON`, the agent fixed it and the second call succeeded — the error-as-string design working as intended.
- Contract quality: all 20 columns have a rationale; 7 dataset-level assumptions written by the steward (USD not cents for CashbackAmount; percentage points for OrderAmountHikeFromlastYear; row count + sha256 identify the file; nulls kept in 7 columns, imputation is the modelling crew's job; 557 duplicate records removed; protected attributes not features; id not a feature, Churn is the label). Measured fields identical to Yoran's M2 contract (gate check 4); `produced_by` is now `analyst_crew`.
- Data artifacts (`clean_data.csv`, `cleaning_report.json`, `stats.json`) still byte-identical to M1. Steward prose is the model's; one inference to note: it describes Tenure 0 as "joined within the last month", which is a reading of the data dictionary, not a measured fact.
- Cumulative cost of the analyst crew so far (M3 + M3b runs): $0.054.

## 2026-09-08 - M2 follow-up: D-M2-3 (from Dor's review of PR #7)
- Review finding, correct: `classify_dtype`'s docstring cited D-M2-1 for the pandas-3 dtype tolerance, but D-M2-1 is the line-endings decision. The tolerance was planned as D-M2-1 early on, lost its number when M1's lesson already covered the pandas fact, and the docstring was never updated. It points at an unrelated decision.
- **D-M2-3: `str`, `object` and `category` dtypes all map to the contract dtype `category`.** pandas 3 gives text columns the `str` dtype rather than `object` (already recorded as an M1 lesson), and a categorical column is text as far as the contract cares. Alternative: add a fifth contract dtype (`"string"`) so the four pandas dtypes map one-to-one. Cost of that alternative: it changes the contract format that Crew 2 and the M6 app read, for a distinction neither of them can act on - a column is either text or a number to them. Cost of the chosen option: a contract built from a pandas-2 frame and one built from pandas 3 are indistinguishable, which is the point.
- Docstring now cites D-M2-3. No behaviour change; gate M2 still 5/5.
- Not changed, per the same review: `range` uses `math.isclose` without `abs_tol`, so a value like -1e-15 in a column whose min is 0 fails. Dor's call to leave it - our writers never produce that, and a negative count should fail.
## 2026-09-08 - M4: Crew-2 tools (features, training, evaluation, model card) - GATE PASSED (6/6)
- Branch `feat/m4-scientist-tools` (Yoran, track B). `hv/features.py`, `hv/evaluate.py`, `hv/train.py`, `hv/model_card.py`, `scripts/run_crew2_tools.py`, gate M4. 55 new tests (176 total).
- Crew 2 read only `dataset_contract.json` and `clean_data.csv`, and the runner validates one against the other before training - 65 checks pass, and it exits 2 without training if they ever disagree.
- Model inputs: 13 numeric + 3 categorical raw features (roles from the contract) + 4 engineered (`cashback_per_order`, `orders_per_tenure_month`, `coupon_rate`, `recency_bucket`). A rate whose denominator is 0 or missing is missing, never zero or infinite. Nulls (1,675, in 7 columns) are imputed inside the Pipeline, per fold.
- Results on 5,073 rows, 5 stratified folds, seed 42: baseline (majority) accuracy 0.8342, roc_auc 0.5, precision@top10 0.1658. logreg roc_auc 0.8913 +/- 0.0169 (p@top10 0.7712), random_forest 0.9770 +/- 0.0086 (0.9487), **hist_gb 0.9857 +/- 0.0051 (0.9724) - served**. Of the 507 customers the model ranks highest, 97.24% really churned, against 16.58% by chance. Full run: 23 seconds.
- **Leakage check, because the plan warns that roc_auc near 1.0 is a red flag.** No feature correlates with the label above 0.38 (`orders_per_tenure_month` 0.38, `Tenure` -0.34, `Complain` 0.25). Permutation importances: Tenure 0.0889, Complain 0.0209, NumberOfAddress 0.0090 - the same two drivers M1's EDA found independently. 242 duplicate feature rows remain (4.8%) after M1 dropped 557 duplicate records; documented as an accepted limit, not fixed here.
- Fairness on the protected columns (never inputs, measured anyway): recall 0.8647 (Male, n=3,047) vs 0.8900 (Female, n=2,026); by marital status 0.8675-0.9032. The one visible gap is the flag rate - Single 0.2415 vs Married 0.1104 - which tracks the real churn rates M1 measured (26.7% vs 11.3%) rather than a bias the model invented.
- **D-M4-1: `train_all` takes an optional `protected` frame.** §3.7 requires a fairness block; §3.6's features frame deliberately excludes Gender and MaritalStatus, so they cannot be measured from it. Alternative: put the protected columns back into features.csv (rejected - the whole point is that they are not there). Cost: one optional parameter; without it the fairness block is empty and says so rather than being invented.
- **D-M4-2: `trained_at` is not in `metrics.json`.** §3.7 lists the key, but CLAUDE.md requires metrics.json to be byte-identical across runs and a clock reading makes a file differ from itself. Alternative: keep the key and have the gate compare everything except it (rejected - weakens the strongest check we have). Cost: the timestamp moved to `run_meta.json`, next to it, the same shape Dor used for the crew runs.
- **D-M4-3: every CSV is read with `float_precision="round_trip"`.** pandas' default C float parser is not correctly rounded on all platforms: reading `clean_data.csv` on macOS produced 31 `CashbackAmount` values one ULP from the Windows reading, which moved 25 numbers in `metrics.json` and made gate M4 fail 5/6 on macOS while passing 6/6 on Windows. round_trip parsing reproduces `float(text)` on every platform (checked on all 5,073 rows). Alternative: round the affected columns after reading (rejected - it hides the problem for the columns we thought of and leaves the next one). Cost: `READ_CSV_KW` in `hv/config.py` must be used by every reader, including the M5 tools; a plain `pd.read_csv` is now a bug. Found in Dor's review of #15, fixed by a commit pushed to the same branch; `artifacts/crew2/` was rebuilt with the fix, which moved 25 third-decimal numbers (random_forest and the fairness block). The served model and the headline numbers did not change. Verified on Windows after pulling the fix: all nine `read_csv` call sites in `hv/`, `crews/` and `scripts/` carry `READ_CSV_KW`, and `features.csv` is now byte-identical on both machines (sha256 `c59c1119727d`).
- Bug caught by reading the real output rather than the code: `_importance_table` sorted nothing. `metrics.json` is written with `sort_keys=True`, so importances come back alphabetically and the model card's "top ten features" was an alphabetical slice. Ranking now happens in the renderer, with a test that feeds it an alphabetical dict.
- `tests/test_health.py` asserted `model_loaded is False` with the comment "no model artifact before M4". M4 publishes that artifact, so the assertion now checks that `/health` tells the truth about the file. The live app will report `model_loaded: true` after this merges.
- Gate M4 6/6. All five gates green on Windows: M0 5/5, M1 7/7, M2 5/5, M3 8/8, M4 6/6. (True as written, against the artifacts built here. After D-M4-3 rebuilt them on macOS this reads 5/6 on Windows - see the 2026-09-09 follow-up below.)
- Note for M5: the validator's `features` check expects the **raw** features the contract declares. The four engineered columns are derived and not declared, so they must not be passed as `required_features`.
- Next: M5 - the scientist crew and the Flow, together.

## 2026-09-09 - M4 follow-up: D-M4-3 (from Dor's review of PR #15) - GATE M4 5/6 on Windows, one incident open
- Review finding, correct: `gate.py --m 4` failed 5/6 on macOS while passing 6/6 on Windows. pandas' default C float parser is not correctly rounded on every platform, so macOS read 31 `CashbackAmount` values one ULP away from the Windows reading, which moved 25 numbers in `metrics.json`. Dor pushed the fix to `feat/m4-scientist-tools` himself as one ordinary commit rather than handing the finding back, and rebuilt `artifacts/crew2/` on macOS.
- D-M4-3 itself is recorded with D-M4-1 and D-M4-2 in the M4 entry above, where the decisions live. Dor and I wrote it independently at the same time; his wording is the one kept.
- Numbers in this report and in the M4 entry above were re-read from the committed `artifacts/crew2/metrics.json`, not retyped. Headline unchanged: served `hist_gb`, roc_auc 0.9857 +/- 0.0051, precision@top10 0.9724, baseline 0.5 / 0.1658, and the leakage argument (no feature above |r| 0.38). What moved: the whole `random_forest` row, the fairness block, and - not in Dor's list - `hist_gb` accuracy 0.9689 -> 0.9691.
- **Incident, open, found while verifying the fix: `metrics.json` still differs between the two machines, and the CSV reader is not the cause.** `features.csv` now matches byte for byte on both (sha256 `c59c1119727d`), so D-M4-3 did its job, but gate M4 still fails here on "metrics.json differs between runs". 11 fields differ, every one of them `random_forest` (roc_auc, accuracy, f1, precision, recall, pr_auc, means and stds), by 1e-4 to 3.5e-3; `hist_gb`, `logreg`, the baseline, fairness and importances are identical.
- Ruled out by experiment rather than by argument: each machine is internally deterministic (two runs here are byte-identical); the model is already `random_state=42, n_jobs=1`; the installed versions match the pins exactly (scikit-learn 1.9.0, numpy 2.4.6, pandas 3.0.5, Python 3.11.16); perturbing one input cell by one ULP does not move `random_forest` at all, and perturbing all 55,803 numeric cells by one ULP moves nothing. The difference is therefore produced inside sklearn's tree building on two different CPU architectures (Windows AMD64 vs Apple Silicon), where a last-bit difference in an impurity sum flips one split and changes a whole subtree. No seed and no `read_csv` keyword can remove it.
- Decision still needed with Dor (M4 merged as PR #15/#19 while this was being written, so the gate is now red on one of the two machines with the code already on main), cheapest first: **(a)** compare `metrics.json` across machines with a numeric tolerance while keeping byte-identity for two runs on one machine, which is what PLAN's "two runs" rule actually meant - recommended; **(b)** keep `random_forest` in the report but out of the byte-identity check; **(c)** accept that only whoever built the artifacts can run gate M4 green, which is the current state and the worst of the three.
- Related gap: `.github/workflows/ci.yml` runs `ruff` and `pytest` only, never `gate.py`. A third machine (ubuntu x86-64) running the gate in CI would have caught both this and D-M4-3 the moment they appeared, and would settle the architecture question. Proposed to Dor, his file.
- State on Windows after the fix: 176 tests green, ruff clean, M0 5/5, M1 7/7, M2 5/5, M3 8/8, **M4 5/6**.
## 2026-09-09 — M6: the app (five pages + the break-it panel) — GATE 5/7 local, live checks pending deploy
- Branch `feat/m6-app`, PR #17. `app/artifacts.py` (reads the published artifacts, reports `available` instead of raising), `app/routes.py` (five pages + `POST /api/validate`), templates, `static/style.css`, `static/app.js`, gate M6. 29 new tests (205 total).
- **Scope deviation, logged:** M6 was built before M5. PLAN.md orders M5 (the Flow) first, but M5 is the joint milestone and it was blocked on Yoran's M4; Dor asked for M6 in the meantime. M5 remains next and is now split explicitly (issue #18).
- Design decision (D-M6-1): the app is a customs checkpoint, not a dashboard — goods on one side, declaration on the other, an inspector's stamp between them. Barlow Condensed (signage) + Public Sans (the typeface of official forms) + IBM Plex Mono for aligned digits; port concrete and steel, with saturated colour reserved for the two verdicts. Alternative considered: the usual card-grid dashboard (rejected: it says nothing about what this project is). Cost: a bespoke stylesheet instead of a framework, about 260 lines.
- `POST /api/validate` reuses `hv.contract.validate` through the same tamper-a-copy path as `scripts/break_it.py`; the published artifacts are never written to. Six presets, or an uploaded CSV, capped at 8 MB.
- Found only by screenshotting the rendered page: Chrome's auto dark mode re-tints any page that does not declare `color-scheme`, so the palette rendered grey until `color-scheme: light dark` was added. Reading the CSS would not have caught it.
- The modelling page degrades honestly while `artifacts/crew2/` is absent from main, and `app/routes.py` imports `READ_CSV_KW` with a fallback until M4 merges (one line to delete then).
- Gate M6: 5/7. The two live checks fail on purpose — Railway still serves the M0 skeleton, so `/analyst` and friends are 404 there. They pass after this merges and Railway redeploys; result to be posted then. Not verified: real mobile width (the browser resize did not reach the tab); the layout collapses to one column under 60rem by construction and there is no horizontal overflow at desktop width.
- Work split for the rest recorded in issue #18: M5 together (Yoran `crews/scientist/`, Dor `flow/`, meeting at `run_scientist_crew` / `run_scientist_stub`), M7 Dor, M8 Yoran.
- **M6 closed.** PRs #15 (M4), #19 (M4 docs, reopened after its base branch was deleted) and #17 (the app) merged in that order. `artifacts/crew2/` is on main, so the model page fills itself from the published metrics; the temporary `READ_CSV_KW` fallback in `app/routes.py` is gone. **GATE M6: PASS 7/7** after the Railway redeploy (commit b1c09be). All five presets plus `unit_change` were exercised against the live site and every one was refused: unit_change → integrity+range, rename_column → columns+integrity, drop_contract_field → columns, bad_category → integrity+values, dtype_change → dtype+integrity, row_loss → integrity+rows. Gates on main: M1 7/7, M2 5/5, M3 8/8, M4 6/6, M6 7/7.
- Merge lesson, second time: a docs PR based on a feature branch is auto-closed by GitHub when that branch is deleted on merge (#9 and #16 both died this way). Docs PRs must target `main`. Noted for Yoran in #19 and in issue #18.
- Before merging #16 its numbers were corrected to the rebuilt artifacts and the D-M4-3 entry was added, because `hv/config.py` cites that id — leaving it undocumented would have repeated the dangling-reference finding from the #7 review.

## 2026-09-09 — M4 follow-up resolved: D-M4-4, and the gates now run in CI
- Yoran's #20 was right, including about my own summary: `hist_gb` accuracy moved 0.9689 → 0.9691 and it was in the diff I produced, while I wrote that the served model's numbers were unchanged — I had checked roc_auc and precision@top10 only. Re-reading the file is the rule for a reason.
- Verified here before deciding: two fresh runs on this machine are byte-identical for both files, and the committed artifacts match a fresh run here exactly. So each machine is internally deterministic and the disagreement is strictly between machines. With his ULP experiments (perturbing one cell, then all 55,803, moves `random_forest` not at all) the input is ruled out; the difference is produced inside sklearn's Cython tree building, where a last-bit difference in an impurity sum on another CPU flips a split. `random_state` fixes the sampling, not the arithmetic.
- **D-M4-4: byte-identity within a machine, a numeric tolerance across machines.** Gate M4's old single check became two: "two runs on this machine are byte-identical" (both files, the real determinism claim, which is what PLAN's "two runs" means) and "matches the committed artifacts" — bytes for `features.csv`, and `metrics.json` compared field by field with `METRICS_TOLERANCE = 0.005`, printing how many fields differ and the worst one on every run. Alternatives: keep `random_forest` out of the comparison (rejected — it hides the whole variant rather than bounding it); accept that only the builder can run the gate green (rejected — that tests the machine, not the code). Cost: a genuine regression smaller than 5e-3 in `random_forest` would now pass, which is why the worst delta is always printed. Gate M4 is 7/7 here.
- **CI now runs the gates** (his suggestion, and the best thing in that PR): `ci.yml` ran ruff and pytest only, so no third machine had ever executed a gate — both this incident and D-M4-3 would have been caught on the day they appeared. Gates 1, 2, 3, 4 and 6 now run on ubuntu x86-64 on every push; the live checks skip themselves because `LIVE_URL` is unset there. This also settles the architecture question by experiment: if ubuntu matches Yoran's Windows numbers and both differ from macOS, the cause is the CPU and nothing else.
