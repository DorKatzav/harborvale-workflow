# Copy to .env (gitignored). Never commit real values.

# OpenAI key for the agents (needed from M3; on Railway only from M7)
OPENAI_API_KEY=

# Public Railway URL, used by scripts/gate.py live checks (M0)
LIVE_URL=

# Password for the live full run page (M7)
APP_PASSWORD=
